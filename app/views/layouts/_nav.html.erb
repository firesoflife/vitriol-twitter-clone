<nav class="navbar is-danger">
  <div class="navbar-brand">
    <%= link_to root_path, class: 'navbar-item' do %>
      <h1 class="title is-5">Vitriol!</h1>
    <% end %>
    <div class="navbar-burger burger" data-target='navbarExample'>
      <span></span><span></span><span></span>
    </div>
  </div>

  <div id="navbarExample" class="navbar-menu">
    <div class="navbar-end">
      <div class="field is-grouped">
        <p class="control">
          <%= link_to 'New Bark', root_path, class: 'button is-warning is-inverted' %>
        </p>
        <% if user_signed_in? %>
          <p class="control">
            <%= link_to current_user.name, edit_user_registration_path, class: 'button is-warning is-inverted' %>
          </p>
          <p>
            <%= link_to 'Logout', destroy_user_session_path, method: :delete, class: 'button is-warning is inverted' %>
          </p>  
        <% else %>
          <p class="control">
          <%= link_to 'Sign In', new_user_session_path, class: 'button is-warning is-inverted' %>
        </p>
        <p class="control">
          <%= link_to 'Sign Up', new_user_registration_path, class: 'button is-warning is-inverted' %>
        </p>
        <% end %>
      </div>
    </div>
  </div>
</nav>